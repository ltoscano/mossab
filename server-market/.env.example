# Marketplace Server Configuration

# Server port
PORT=4000
MARKETPLACE_PORT=4000

# Database path (SQLite)
DB_PATH=./data/marketplace.db

# Admin user (for initial setup)
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@mossab.local
ADMIN_PASSWORD=changeme123

# Security
NODE_ENV=production

# Optional: PostgreSQL (for future migration)
# DATABASE_URL=postgresql://user:password@localhost:5432/mossab_marketplace
